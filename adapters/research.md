# Everloom Adapter: Research

When the task involves research:

- preserve source URLs, titles, dates, and provenance;
- distinguish fact, inference, hypothesis, and contradiction;
- record search gaps and failed leads;
- prefer primary sources when available;
- require source support before marking factual findings verified;
- store durable summaries instead of relying on transient chat context.
